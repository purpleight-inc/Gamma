import { FooterObservDirective } from './footer-observ.directive';

describe('FooterObservDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterObservDirective();
    expect(directive).toBeTruthy();
  });
});
