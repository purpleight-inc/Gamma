import { LoadObservDirective } from './load-observ.directive';

describe('LoadObservDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadObservDirective();
    expect(directive).toBeTruthy();
  });
});
